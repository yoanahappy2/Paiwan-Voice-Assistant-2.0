# 排灣語 SFT 訓練依賴
# 在清華 GPU 集群上安裝：pip install -r requirements_cluster.txt

# 核心框架
torch>=2.1.0
transformers>=4.37.0
peft>=0.8.0
bitsandbytes>=0.42.0
accelerate>=0.27.0

# 數據處理
datasets>=2.17.0

# 訓練工具
deepspeed>=0.12.0

# 可選：TensorBoard
tensorboard

# 可選：Weights & Biases
wandb
